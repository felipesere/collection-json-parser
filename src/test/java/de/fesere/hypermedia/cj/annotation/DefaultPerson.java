package de.fesere.hypermedia.cj.annotation;

public class DefaultPerson extends BasePerson {
    public DefaultPerson(String name, int age, int id, boolean isAdmin, double someValue) {
        super(name, age, id, isAdmin, someValue);
    }
}
